dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install mono-xbuild
    - sudo apt-get install doxygen
test:
  override:
    - xbuild
    - echo "finished"
deployment:
  production:
    branch: master
    commands:
      - ./generate-docs.sh
